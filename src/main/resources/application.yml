server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://192.168.148.3:3306/campus_market?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 10MB

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: assign_id   # 雪花算法生成ID

jwt:
  secret: campus-market-secret-key-must-be-long-enough-for-security
  expiration: 86400000   # 24小时

